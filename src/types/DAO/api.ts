export type DAOCities = {
    id: number;
    city: string;
}

export type DAOEventCard = {
    id: string;
    event: string;
    description: string;
    date: string;
    time: string;
    image: string;
    place: string;
    address: string;
}